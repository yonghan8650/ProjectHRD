<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.bswill.mapper.OrganizationMapper">
   
   <select id="orgList"  resultType="com.bswill.domain.OrganizationChartVO">
	SELECT employee_id, profil, JOB_ID, DEPTID, emp_tel, emp_mail, STATUS
	FROM tbl_employee;
   </select>
   
   <update id="checkFavorite">
        UPDATE tbl_employee SET favorite = NOT favorite WHERE employee_id = #{employeeId}
    </update>
   
   
</mapper>