<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 패스워드 가져오기 -->
<mapper namespace="com.bswill.mappers.CommonMapper">
	<select id="selectPass" resultType="String">
		select passwd from tbl_employee where employee_id = #{employee_id}
	</select>
	
	<update id="updatePass" parameterType="java.util.Map">
		update tbl_employee set passwd = #{newPw} where employee_id = #{employee_id}
	</update>
</mapper>