<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.bswill.mapper.SalaryMapper">

    <!-- 급여 목록 조회 -->
    <select id="selectSalaryList" resultType="com.bswill.domain.SalaryVO">
       select * from tbl_salary
       order by employee_id asc
    </select>
    
    <!-- 급여년월에 따른 급여 조회(전체) -->
<!--     <select id="selectSalarySeach" resultType="com.bswill.domain.SalaryListVO">
       select * from tbl_salarylist
       where pay_yearmonth = #{keyword}
       order by employee_id asc
    </select> -->
    
    <!-- 급여년월에 따른 급여 조회 -->
    <select id="selectSalarySeach" resultType="map">
		select pay_yearmonth, tbl_employee.employee_id, emp_name, JOB_ID, salary, premium, sum
		from tbl_employee, tbl_salarylist
		where tbl_employee.employee_id = tbl_salarylist.employee_id 
		and DATE_FORMAT(pay_yearmonth, '%Y-%m') = #{keyword};
    </select>
    
</mapper>
